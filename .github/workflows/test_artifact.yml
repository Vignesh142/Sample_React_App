name: Test Artifact Availability

on:
  workflow_dispatch: # ✅ Manually trigger this

permissions:
  contents: read
  actions: write

jobs:
  test-artifact:
    runs-on: ubuntu-latest
    steps:
      - name: List Artifacts
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🔎 Checking available artifacts..."
          gh run view ${{ github.run_id }} --log
